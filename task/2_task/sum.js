/**
 * Create a custom module that exports a function which takes two parameters and returns their sum. Then
 * use this module in another file to perform some arithmetic operations.
 **/

// creating and exporting a function sum().
module.exports = function sum(a, b) {
  return a + b;
};
